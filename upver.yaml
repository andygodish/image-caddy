version:
  file: version.txt
  pattern: '^([^[:space:]]+)[[:space:]]*$'
  group: 1

upstream:
  file: Dockerfile
  pattern: 'ARG CADDY_VERSION="([0-9]+\.[0-9]+\.[0-9]+)-alpine"'
  group: 1

changelog:
  file: CHANGELOG.md
